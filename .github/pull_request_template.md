## Token submission

- Chain ID:
- Token contract address:
- Official project website:
- Official source confirming the contract address:
- Logo source:
- Private token evidence, when `private` is `true`:
- CoinGecko page, when `cgCoinId` is supplied:
- Market-data reference explanation, when supplied:

## Checklist

- [ ] This pull request adds or updates only one token directory.
- [ ] The token directory uses the lowercase contract address.
- [ ] The directory contains only `metadata.json` and `logo.png`.
- [ ] `metadata.json` does not contain `logoUrl`.
- [ ] The logo is a square PNG no larger than 512 KiB.
- [ ] I ran `npm run tokens:validate` and `npm run tokens:build` locally.
- [ ] I understand that inclusion is not a security audit or endorsement.
