# Chains

Create one directory per numeric chain ID. Inside it, create one directory per token using the lowercase contract address.

```text
<chainId>/<lowercase-token-address>/
├── metadata.json
└── logo.png
```
